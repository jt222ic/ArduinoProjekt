SYMBOLS = ABCDEFGHIJKLMNOPQRSTUVWXYZÅÄÖ
 
def get_mode()
    while True
        print(Encrypt  Decrypt)
 
        response = input( ).lower()
 
        if response.startswith(e)
            return encrypt
        elif response.startswith(d)
            return decrypt
 
        print(Invalid input)
 
 
def get_key()
    # få nyckel från användare
    while True
        print(Please enter the key (0 to {}) .format(len(SYMBOLS) - 1))
        response = input(Enter your key )
 
        if response.isdigit()
            key = int(response)
            if 0 = int(response)  len(SYMBOLS)
                return key
 
        print(Invalid key)
 
 
def translate(mode, key, message)
    translated = 
 
    for symbol in message
        if symbol in SYMBOLS
            num = SYMBOLS.find(symbol)
            #print({} = {}.format(symbol, num))
 
            if mode == encrypt
                num += key
            else
                num -= key
 
            if num = len(SYMBOLS)
                num = num - len(SYMBOLS)
            elif num  0
                num = num + len(SYMBOLS)
 
            translated = translated + SYMBOLS[num]
 
        else
            translated = translated + symbol
 
    return translated
 
 
def main()
    mode = get_mode()
    key = get_key()
 
    print(Enter the message )
    message = input( ).upper()
 
    result = translate(mode, key, message)
 
    print(n + =30)
    print(fLäge         {mode})
    print(fNyckel       {key})
    print(fMeddelande   {message})
    print(fResultat     {result})
 
if __name__ == __main__
    main()